#include <bits/stdc++.h>
#include<iomanip>
using namespace std;

#define fastio() \
    ios::sync_with_stdio(false); \
    cin.tie(nullptr); \
    cout.tie(nullptr)

typedef long long ll;

int main() {
    fastio();
    double r;
    cin >> r;
    cout << fixed << setprecision(9);
    cout << pi*r*r<< endl;
    return 0;
}
